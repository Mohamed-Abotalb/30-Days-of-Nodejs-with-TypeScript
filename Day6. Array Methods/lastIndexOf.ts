const arr: number[] = [5, 2, 8, 5, 6];
console.log(arr);

// Search for value 5 and get its index from the last of the array
const pos: number = arr.lastIndexOf(5);
console.log('index of 5 is : ' + pos);

// Search for value 8 and get its index from the last of the array
const pos1: number = arr.lastIndexOf(8);
console.log('index of 5 is : ' + pos1);

// Search for value 9 and get its index from the last of the array
const pos2: number = arr.lastIndexOf(9);
console.log('index of 5 is : ' + pos2);
